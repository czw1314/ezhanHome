import React from 'react'
import {Select, Button, DatePicker, Table, Input, message, Modal, InputNumber} from 'antd';
import locale from 'antd/lib/date-picker/locale/zh_CN';
import moment from 'moment';


class Home extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        const Option = Select.Option;
        return (
            <div className='home'>
ll
            </div>
        )
    }
}

export default Home